version: '3.7'

services:
  web:
    restart: always
    build:
      context: .
      dockerfile: ./Dockerfile
    command: gunicorn wsgi --worker-class gevent -w 1 --bind 0.0.0.0:8000 --reload
    volumes:
      - ./:/usr/src/app/
    expose:
      - "8000"
    ports:
      - "8000:8000"
